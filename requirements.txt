# Core dependencies for File Comparison Tool
pandas>=1.5.0,<3.0.0
openpyxl>=3.0.0,<4.0.0
xlrd>=2.0.0,<3.0.0

# GUI dependencies (tkinter is built-in with Python)
# No additional GUI dependencies required

# Performance and monitoring
psutil>=5.9.0,<6.0.0
memory-profiler>=0.60.0,<1.0.0